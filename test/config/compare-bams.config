params {
    input_csv = "path/to/input/csv/"
    output_dir = "where/to/save/outputs/"
}

process {
    cache = true
    executor = "slurm"
    queue = "slurm partition type"
}

docker {
    enabled = true
}
